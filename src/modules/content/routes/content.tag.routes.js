const { Router } = require("express");
const tagController = require('../controllers/content.tag.controller');
const router = Router();


// router.get("/:tagSlug", tagController.getTopicContent);

module.exports = router;
