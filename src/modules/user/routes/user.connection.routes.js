const { Router } = require("express");
const userController = require("../controllers/user.connection.controller");

const router = Router();

// router.post("/update-personal-info", userController.updatePersonalInfo);

module.exports = router;
  